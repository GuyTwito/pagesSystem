{
  "name": "pagessystem",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "installserver": "cd server && npm install",
    "installclient": "cd client && npm install",
    "installproject": "npm install && npm run installserver && npm run installclient",
    "client": "cd client && npm run serve",
    "server": "cd server && npm run start",
    "project": "concurrently \"npm run server\" \"npm run client\""
  },
  "keywords": [
    "react"
  ],
  "author": "guy twito",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0"
  },
  "devDependencies": {}
}
